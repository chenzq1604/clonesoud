# 后端运行环境依赖（conda 环境 av-clone）
# 安装：pip install -r requirements.txt

# ===== Web 框架与服务 =====
fastapi==0.115.0
uvicorn[standard]==0.30.6
# FastAPI 的 Form / UploadFile（multipart 表单）解析
python-multipart==0.0.12

# ===== HTTP 客户端 =====
# 火山方舟 API（文生图）/ 本地 ComfyUI / 本地 CosyVoice 调用
httpx==0.27.2
# ComfyUI WebSocket 实时进度监听（comfyui_video.py 直接使用；
# 虽会被 uvicorn[standard] 一并安装，直接依赖应显式声明）
websockets==17.1

# ===== 数据库（SQLite 异步） =====
sqlalchemy==2.0.35
aiosqlite==0.20.0

# ===== 配置与校验 =====
pydantic==2.9.2
pydantic-settings==2.5.2

# ===== 图像处理（演示模式占位图生成） =====
Pillow==10.4.0

# ===== 测试依赖（运行 pytest 需要） =====
pytest==9.1.1
pytest-asyncio==1.4.0
